## Example environment variables for local development
# Copy this file to .env and edit values before using docker-compose

# JWT secret used to sign tokens (do NOT use real secrets in VCS)
APP_JWT_SECRET=change-me-local-placeholder

# MongoDB connection used by the application when running inside docker-compose
SPRING_DATA_MONGODB_URI=mongodb://mongo:27017/letsplay

# Spring profile to activate (dev/test/prod)
SPRING_PROFILES_ACTIVE=dev
